<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= heading %></title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>

  <div class="container">
    <h1><%= heading %></h1>
    <form action="<%= post ? '/api/posts/' + post.id : '/api/posts' %>" method="POST">
      <label for="post">Title:</label>
      <input type="text" id="post" name="post" value="<%= post ? post.post : '' %>" required />
      
      <label for="description">Description:</label>
      <textarea id="description" name="description" required><%= post ? post.description : '' %></textarea>
      
      <label for="writer">Writer:</label>
      <input type="text" id="writer" name="writer" value="<%= post ? post.writer : '' %>" required />
      
      <button type="submit"><%= submit %></button>
    </form>
    <a href="/" class="btn">Back to Posts</a>
  </div>

</body>
</html>
